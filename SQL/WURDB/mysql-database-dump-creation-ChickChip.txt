mysql -u mit -p
drop database `chickchipdb`;
flush privileges;
CREATE DATABASE IF NOT EXISTS Phyloarray_Chick;

5. Exit MySQL interface, enter:

exit;

6. Load the dump into the database. Note: "chickchipdb" database name was already defined in SQL dump (phyloarray_chick2nd_20120323_0844.sql) and trying to change it here caused failures in R package

#See install.dump.sh for the latest dump installations

7. Test R package
#TODO
#library(microbiome)
#params <- run.profiling.script(dbuser = "chick", dbpwd = "passu", dbname = "chickchipdb")




